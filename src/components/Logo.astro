---
---

<!-- Replace this SVG with your custom art later -->
<svg 
  xmlns="http://www.w3.org/2000/svg" 
  viewBox="0 0 100 100" 
  fill="none" 
  class="w-8 h-8 text-pink-400 animate-spin-slow" 
>
  <circle cx="50" cy="50" r="40" stroke="currentColor" stroke-width="5" />
  <path d="M50 10 L60 50 L50 90 L40 50 Z" fill="currentColor" />
</svg>

<style>
  .animate-spin-slow {
    animation: spin 10s linear infinite;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }
</style>
